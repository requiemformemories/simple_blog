<%= form_for @category, :url => skvsdovlmfd_categories_path do |f| %>

   <%= render :partial => 'form', :locals => {:f => f } %>
    
    <%= f.submit "Create", :class => 'btn btn-primary' %>

<% end %>